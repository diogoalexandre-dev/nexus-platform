# Matching

Lógica de correspondência entre o briefing do contratante e os prestadores disponíveis
(categoria, região, disponibilidade, avaliação). Consome `server/repositories` e expõe
funções que `app/` e `server/services` podem chamar.
